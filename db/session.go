package db

// BoltSession is the struct that holds the data for our Bolt sessions
type BoltSession struct {
}

// RedisSession is the struct that holds the data for our Redis sessions
type RedisSession struct {
}
